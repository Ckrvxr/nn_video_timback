# Environment

Package/environment manager: **pixi**. Never use `pip`, `python`, or `python -m` directly.
- Run scripts: `pixi run <script>`
- Execute Python: `pixi run python ...`
- Install packages: `pixi add <pkg>`
- Run pytest: `pixi run pytest ...`
