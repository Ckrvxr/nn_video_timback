"""Sharpness-Aware Minimization — not needed in JAX, stub kept for imports."""


class SAM:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("SAM is not ported to JAX. Use optax optimizers directly.")
